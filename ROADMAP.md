# Universal Harvester — проект плана развития (черновик)

> Документ — предложение по развитию. Приоритеты и состав фаз согласуются с владельцем.
> Связанные документы: [`ARCHITECTURE.md`](ARCHITECTURE.md), [`HARVESTER_DSL.md`](HARVESTER_DSL.md),
> [`PARSING.md`](PARSING.md), [`ADDING_SOURCES.md`](ADDING_SOURCES.md).

## 0. Статус: минимально рабочая версия (MVP) — зафиксировано (тег `v0.1.0-mvp`)

Подтверждено на стенде и зафиксировано как MVP:
- **Ядро**: ленивые импорты, единые сигнатуры коннекторов, прерывание прогона на ошибке шага.
- **DSL-петля целиком**: `DEF / CALC / GET (source) / GET script / GET APPLY / PRINT / SHOW / SAVE / NOTIFY`;
  парсер с тестами (всего ~81 тест).
- **Коннекторы**: TheHive, NetBox (4.x), Jira SM (issues/changelog/comments/worklogs/attachments/issuelinks/CMDB),
  in-memory SQL (sqlite3/duckdb), pandas и др.
- **Секреты** (CRUD), **Objects** (версионирование source/script/notifier/llm).
- **UI**: persistent-вкладки без мельканий; раздельные индикаторы Harvester/AI.
- **History**: запись запусков (script/steps/duration/user/agent), просмотр и поиск по тексту.
- **AI-агент**: выбор LLM (ollama/openai-совм.) + health + контроль контекста; чат; ReAct-агент,
  который сам пишет/запускает/проверяет скрипты; контекст по запросу (действия list_sources/
  get_source_functions/list_objects/search_objects/get_object/run) + логирование запросов (токены/модель/пользователь).

Известные ограничения MVP: SQL-инъекции приняты как риск (доверенный круг); master key захардкожен в `front.py`;
`universal_harvester`/`grafana`/`teleport` отключены/не перенесены; REST/MCP-слой наружу не сделан;
CMDB Jira не протестирован (права). Дальнейшее развитие — фазы ниже.

## 0.1. Что добавлено в `v0.2.0` (раздел Settings)

Реализован раздел **Settings** (см. [`SETTINGS_PLAN.md`](SETTINGS_PLAN.md)):
- **Внешний вид**: тема (тёмная/светлая), шрифты интерфейса/таблиц, тема CodeMirror — персонально (settings, scope `user:<username>`).
- **Цвета**: пользовательская палитра по теме; карточки/панели/AG Grid/Quasar dark/CodeMirror следуют теме и цветам.
- **Учётная запись (self-service)**: смена своего пароля и метаданных.
- **Принудительный логаут**: per-user session-epoch; смена пароля/блокировка отзывают сессии (страж `ui.timer`).
- **Админ-УЗ** (`fullmaster`/`useradmin`): AG Grid с фильтрами, создание/блокировка/роли/сброс пароля/метаданные.
- **AI-настройки + журнал** (`fullmaster`/`aiadmin`): лимит действий агента (`settings.global`), таблица `ai_log`, сводка/детализация токенов.
- **Разрешённые сети** (`fullmaster`/`netadmin`): управление `access_networks` (CIDR-whitelist).
- Хранилище настроек: таблица `settings(scope,key,value)`.

## 0.2. Что добавлено в `v0.3.0` (HTTP API)

**HTTP API** (см. [`API.md`](API.md)): `POST /api/script` — выполнение DSL-скрипта по API-ключу; ответ
текстом (PRINT) или zip (`output.txt` + PNG из `SHOW matplotlib` + файлы из `SAVE`). Запуск в контексте
владельца ключа (его роли). Аутентификация: `X-API-Key` / `Authorization: Bearer`.

**API-ключи**: таблица `api_keys` (sha256-хэш токена, владелец, комментарий, `enabled`, `created_at`,
`created_by`, `expires_at`). Управление в Settings (роли `fullmaster`/`apiadmin`): создание (токен один раз,
опц. срок жизни в днях), вкл/выкл, удаление; список со статусом (активен/выключен/истёк), временем создания,
автором и датой истечения. `verify_api_key` отклоняет выключенные/истёкшие ключи и ключи заблокированных владельцев.

## 0.3. Мультиязычность (i18n) в `v0.4.0`

Инфраструктура в `app/i18n.py`: `SUPPORTED_LANGUAGES` (en/ru), `TRANSLATIONS` (каталоги по языкам),
`translate(key, lang)` (фолбэк: язык по умолчанию → сам ключ), `resolve_language(saved, accept_language)`.
Язык определяется: сохранённый выбор (`settings`, scope `user:<username>`, ключ `language`; на логине —
`app.storage.user['lang']`) → язык браузера (`Accept-Language`) → английский по умолчанию. Резолвится в
`main_page`/`login_page` (`current_state["lang"]`). Селектор языка — в Settings и на странице логина
(сохранение + перезагрузка). **Добавление языка**: дописать код в `SUPPORTED_LANGUAGES` и словарь в `TRANSLATIONS`.

Переведён весь UI: навигация, **Settings, Secrets, Objects, AI, Harvester, History** и **страница логина**.
Намеренно не локализованы: агентский протокол (вход/выход LLM), технические подписи шагов `_step_label`
(общие с историей/агентом), серверные сообщения и диагностическая панель логина.

Логин в `v0.4.0`: корректное переключение тем (Quasar dark + CSS-переменные), переименование в
«Universal Harvester 2 Scripted», опциональная кнопка входа через Keycloak, селектор языка рядом с
переключателем темы, вход по Enter в поле пароля.

## 0.4. MCP наружу (Трек 2)

Outward-facing **MCP-сервер** `mcp_server.py` (см. [`MCP.md`](MCP.md)) на `fastmcp` — отдельный процесс
против той же БД. Инструменты (все возвращают **структурированный JSON**): `get_dsl_reference`, `run_script`,
`list_sources`, `get_source_functions`, `list_objects`, `search_objects`, `get_object`. Аутентификация по
API-ключу (аргумент `api_key`), запуск в контексте владельца (его роли). Переиспользует `execute_script_api`,
движок и `app/agent_actions.py` (role-aware capability-функции + `run_script_structured` + `DSL_REFERENCE`).
Бинарные артефакты (PNG/файлы) — через REST `POST /api/script`. `fastmcp` опционален.

Для агентов прозрачна модель **source_object → source_type → functions**: `list_objects`/`get_object` отдают
у source-объектов `source_type` и `functions` с параметрами; параметры — с выведенным **типом и примером**
(берётся из карты источников, бедные примеры дополнены). Смоук-проверка — `mcp_smoke.py` (in-memory клиент).

Дальше (не сделано): интерактивный ввод master key вместо захардкоженного (сейчас намеренно для авто-перезапуска
dev-стенда; интерактивный вариант закомментирован в `front.py`).

## 0.5. Доработки UI в `v0.6.0`

- **Settings**: каждый раздел (Язык/Внешний вид/Учётная запись/Пользователи/AI/Сети/API-ключи) —
  сворачиваемый (`ui.expansion`, по умолчанию свёрнут) для облегчения интерфейса.
- **Цвета интерфейса**: добавлены роли **«Шапка»** и **«Кнопки»** (по теме). Шапка и кнопки
  (заполненные/outline/flat) перекрашиваются надёжно через инлайн-`!important` на элементах (JS +
  `MutationObserver` для вновь создаваемых) — перебивает любые правила Quasar/nicegui.

## 0.6. Анализатор выполнения в `v0.7.0`

На вкладке Harvester — окно **«Анализ выполнения»** + кнопка рядом с «Выполнить». `app/analyzer.py`
(`build_execution_mermaid`) строит **Mermaid-граф** потока: DEF/CALC, обращения к source-объектам,
вызовы `GET script:...` (рекурсивно разворачиваются в подграфы), APPLY и элементы вывода
(PRINT/SHOW/SAVE/NOTIFY). Рёбра подписаны именем передаваемых данных; категории раскрашены; линии
светлые (читаемы на тёмном фоне), граф центрирован. Анализ статический (по разбору, без выполнения).

## 0.7. Мелкие доработки в `v0.7.1`

- Редактор скриптов: перенос длинных строк (`line_wrapping` + CSS-фолбэк); поддержка `\`-переноса
  команды на несколько физических строк (`command_parser` склеивает `\`+перевод строки).
- Harvester: единый вертикальный скроллбар на всю панель (карточка-контейнер прокручивается).
- Иконка приложения: `favicon.ico`/`favicon.svg` (воронка-харвестер, неон-тема), генератор
  `tools/make_favicon.py` (чистый stdlib).

## 0.8. Надёжность и UX в `v0.8.0`

- **Ретраи elastic/opensearch**: переиспользуемый `app/sources/additional/retry.py` (`retry_call`,
  экспоненциальный backoff + jitter); requests-путь elastic повторяет запросы (первый + каждую страницу
  пагинации) на сетевых ошибках/таймауте и кодах 429/502/503/504, не на 4xx. Конфиг в source-объекте
  (`max_retries`/`retry_backoff_seconds`/`retry_on_status`/`retry_on_timeout`). Грейсфул-дефолты вместо
  `KeyError`. **Логирование** каждого повтора (WARNING) через `on_retry`.
- **Понятные ошибки параметров GET**: вместо «there is not parameter X» — список недостающих параметров,
  что передано, и сигнатура функции с типами/примерами (из карты).
- **Экранирование markdown** в системных сообщениях Harvester (`_md_escape`): имена с `_`/`*` и т.п. больше
  не интерпретируются как разметка.

## 0.9. Графики matplotlib (общий вид) в `v0.8.1`

- **Общий режим `SHOW(table, matplotlib, {...})`**: `layers=[{kind,x,y,color,label,secondary_y,stacked,...}]`
  — несколько серий/типов на одном графике и вторая ось Y (`secondary_y`); `y` может быть списком столбцов.
- **Пороговые/опорные линии**: `hlines`/`vlines=[{y|x,color,label,linestyle,linewidth}]` (например порог,
  выше которого `bar` — превышение). Принимаются и на верхнем уровне, и внутри слоя.
- **Оформление**: `xlabel/ylabel/grid/legend/legend_loc/logx/logy/xlim/ylim/color`; единая легенда по обеим осям.
- Обратная совместимость сохранена. Документация: `SHOW_MATPLOTLIB.md` (+ ссылка из `HARVESTER_DSL.md`).

## 0.10. Графики 3D/стили, UX редактора и нумерация ошибок в `v0.8.2`

- **3D-графики matplotlib**: `kind=bar3d|scatter3d` по трём осям `x`/`y`/`z` (категориальные оси → подписи
  тиков, `z` — высота/координата); `elev`/`azim`, `bar_width`, `color`. Настройка шрифтов против наезжания
  подписей: `fontsize`/`tick_fontsize`/`label_fontsize`/`title_fontsize`/`tick_rotation`/`labelpad`.
- **Научное оформление**: `style='science'|['science','grid']|'ggplot'|...` (SciencePlots/встроенные стили),
  `rc={...}`, `usetex` (по умолчанию `false` — `science` без LaTeX). Применяется скоупом (`style.context`
  + `rc_context`), без протечки на другие графики. `SciencePlots` добавлен в `requirements.txt` (опц.).
- **Растягивание редактора скрипта** в Harvester: грип на обёртке codemirror — высота тянется мышью,
  нижние элементы сдвигаются (не перекрываются).
- **Номера строк в ошибках валидации**: `command_parser` хранит `line_number` (комментарии не сбивают
  нумерацию); строка показывается в списке ошибок, инфо шага «Валидация», подписях шагов (`L<n>:`),
  ответах HTTP API и MCP-агента.
- **Фикс sqlite3 «too many SQL variables»**: `chunksize` для `to_sql(method='multi')` подбирается от числа
  столбцов (≤900 переменных), широкие таблицы больше не падают при импорте в in-memory.

## 0.11. Надёжность исполнения и временные таблицы в `v0.8.3`

- **Временные таблицы в sqlite3/duckdb**: `get_command_dependency` больше не считает таблицы, создаваемые
  тем же запросом (`CREATE [TEMP] TABLE/VIEW t ... FROM t`), внешними зависимостями (как и CTE `WITH`).
  Раньше такой `GET` навсегда оставался в `pending` (зависимость на свою же `temp` невыполнима), а
  зависимые шаги — следом; `SHOW` получал «нет данных».
- **Упавший шаг не зависает**: `commands_executor` оборачивает выполнение каждого шага — любое исключение
  помечает шаг `error` с текстом, а не оставляет его в `running`; UI дополнительно закрывает «зависшие»
  `running`-шаги при ошибке/исключении.
- **Толерантный `datetime_to_timestamp`** (sqlite3/duckdb): строгий формат → фолбэк на `fromisoformat`
  (микросекунды опц., таймзона со/без двоеточия, суффикс `Z`). Раньше значения без `.%f` молча давали `-1`
  и отфильтровывались условиями вида `MTTA>0`. `sqlite3:query` сообщает `OK (rows N)`.

## 0.12. Версия и безопасность логов в `v0.8.4`

- **Авто-версия в логах**: `app/version.py` `get_app_version()` определяет версию из git-тега
  (`git describe --tags --abbrev=0`) → файл `VERSION` → фолбэк. `front.py`/`mcp_server.py` больше не хранят
  захардкоженную версию — в syslog уходит актуальная (`application_version`).
- **Без пролива секретов в логах GET**: убран `print(executed_command)` (печатал `source_object` с уже
  подставленным секретом `key.value`) → компактный DEBUG `exec GET source:function -> name` без параметров.
- **Сокращение шумного лога**: лог несовпадения типов параметров больше не печатает значения (могут быть
  секретом) — только имя и приведение типов, на уровне DEBUG.

## 0.13. Экспорт схемы, безопасность и Docker-деплой в `v0.9.0`

- **Экспорт схемы выполнения**: во вкладке «Анализ выполнения» кнопки **Экспорт SVG / PNG / Сохранить
  Mermaid**. Клиентский JS берёт отрисованный mermaid-`<svg>` и сохраняет его целиком (SVG — вектор,
  PNG — растеризация всего bbox ×2) — решает проблему скриншота большого графа. Кнопки заливные (видны
  на тёмной теме).
- **Удаление хардкода секретов из исходников**: `mcp_server.py` берёт `db_conf`/master key из окружения
  (`UH2S_DB_CONF`/`UH2S_MASTER_KEY`)/аргументов; `front.py` — `pwinput`/аргументы/env. Добавлен
  [`SECURITY_SCRUB.md`](SECURITY_SCRUB.md) — маскировка секретов во всей git-истории (`git filter-repo
  --replace-text`) и на GitHub, с обязательной ротацией.
- **Развёртывание в Docker**: `Dockerfile` (python:3.12-slim, все зависимости, non-root, версия из
  build-arg), `docker-compose.yml` (сервисы веб 8082 + MCP 8090, тома под sqlite `/data` и сессии
  `.nicegui`), `.dockerignore` (без `.git`/секретов), `env.example`, [`DOCKER.md`](DOCKER.md). `front.py`:
  env-фолбэки секретов, опциональный TLS (HTTPS если есть `crt.pem`+`key.pem`, иначе HTTP за прокси),
  `show` через `UH2S_SHOW`.
- **README**: [`README.md`](README.md) — обзор, индекс документации, запуск в Docker и напрямую в Python,
  модель секретов, TLS, тесты, версия.
- **requirements**: добавлен недостающий `pwinput`.

## 0.14. Персистентный кэш DSL и раздел «Хранилище» в `v0.9.1`

- **Кэш DSL** (задействована таблица `storage`): `SAVE(dataname, storage[, ttl]) AS key`,
  `LOAD(key[, ttl_ignore]) AS name`, `GET LOAD(key[, ttl_flag]):refresh:<ttl> | :not_refresh source:... AS name`
  (read-through: хит пропускает источник, `:refresh` — теневая перезапись при успехе). Общий кэш;
  последовательное чтение-после-записи в одном запуске (линеаризация по ключу). Документация в
  [`HARVESTER_DSL.md`](HARVESTER_DSL.md).
- **LOAD без автоудаления**: истёкшие данные не удаляются при чтении — ошибка с подсказкой (обновить
  или `LOAD(key, true)`). Envelope хранит `created_ts` (сохраняется при перезаписи) и `updated_ts`.
- **Раздел меню «Хранилище»** (только `fullmaster`/`storage_admin`): список записей (ключ, владелец,
  создано/обновлено, TTL, строки, размер, статус), предпросмотр (aggrid), скачивание (json/csv/xlsx),
  удаление, обновление. Новый `storage_list` в `app/db.py`.
- Фиксы: заливные кнопки раздела; устранена коллизия `tr(..., key=...)` (плейсхолдер `{key}`→`{name}`),
  ломавшая Предпросмотр/Удаление и рендер storage-SAVE.

## 0.15. Параллельный исполнитель (Трек 1) в `v0.10.0`

- **Стадийный пул потоков**: независимые команды одной стадии исполняются параллельно
  (`ThreadPoolExecutor`, размер — `--processes`; исполнитель уже в `run.io_bound`). `result_map`
  пишется только собирающим циклом в порядке стадии; ошибка останавливает прогон на границе стадии.
  Мёртвый `multiprocessing.Pool`-блок удалён.
- **По-источниковые лимиты**: `threading.Semaphore` на источник из `source_object.json.max_threads`
  (фолбэк `--processes`), в `current_state["_source_semaphores"]`; оборачивает только вызов источника.
- **Параллельный `APPLY` fan-out**: строки исполняются параллельно (воркеров = `max_threads`/`processes`),
  под тем же семафором; порядок вывода сохраняется (сбор по индексу), `applied_*`/дедуп без изменений.
- **Живой прогресс `APPLY`**: счётчик `k/total` в панели шагов (через `as_completed`), показывается
  и в статусе «выполняется».
- Граф зависимостей и storage-линеаризация не тронуты — чтение-после-записи под пулом сохраняется.
  +3 офлайн-теста (порядок/семафор/ошибка строки).

## 0.16. Планировщик расписаний (Трек 2) в `v0.11.0`

- **Cron-планировщик** (`app/scheduler.py`): минимальный 5-полевой матчер (`cron_matches`/`validate_cron`/
  `next_run`), фоновый daemon-луп раз в минуту в **web-процессе** (тумблер `UH2S_SCHEDULER`), запуск
  сохранённых script-объектов в **контексте владельца** (роли на момент запуска; заблокирован → skip),
  запись в `executions`. Пропущенные при простое — не догоняются; overlap-guard; локальное время.
- **Таблица `schedules`** + CRUD; раздел UI **«Расписания»** (админам `fullmaster`/`schedules_admin`):
  список, форма (выбор script-объекта + валидация cron + превью следующего запуска), вкл/выкл, удаление,
  «Запустить сейчас», обновить (обновляет и список скриптов).
- **ALERT c результатом**: при непустом `return` зашедуленного скрипта пишется syslog ALERT со временем,
  именем и данными (для мониторинга); строго по полю `return`.
- Фиксы: параметризованный INSERT объектов (кавычки в json, напр. `LIKE '123'`, больше не ломают
  сохранение). +12 офлайн-тестов (cron/пустота). i18n RU/EN; заметки в `DOCKER.md`/`README.md`/`env.example`.

## 0.17. Пересборка алертов TheHive в `v0.11.1`

- **`regroup_thehive_alert`** (при `flatten=True`, параметр `regroup`, по умолчанию вкл): `tags_N` →
  один столбец `tags` (текстовый JSON-список значений); группы `customFields_N_*` → столбцы
  `custom_<name>` = `<value>` (префикс `custom_` исключает перезатирание штатных полей; группы без
  `name` пропускаются; сырые `customFields_N_*` удаляются). +5 офлайн-тестов.

## 0.18. FREETEXT-поиск CMDB (jira_sm) в `v0.11.2`

- **`jira_sm:search_cmdb_freetext`** — свободнотекстовый поиск по CMDB Insight через эндпоинт
  `GET /rest/insight-am/1/search?criteria=<text>&criteriaType=FREETEXT&schema=<id>&attributes=<list>&offset&limit`.
  Параметры: `freetext` (обяз.), `schema`, `attributes` (строка/список), `limit`, `search_path`,
  `timeout` (override), `flatten`. Пагинация по `offset`, устойчивый разбор ответа
  (`_extract_cmdb_entries`). Размер страницы 50, WARNING при отсутствии `schema` (иначе скан всех схем
  → таймаут). +4 офлайн-теста.

## 0.19. Инъекция в DEF и обработка ошибок elastic в `v0.11.3`

- **Инъекция `%(name)X` в DEF**: значение `DEF` прогоняется через `process_injections` по уже
  объявленным переменным — `DEF "prod" AS env | DEF "logs-%(env)s-*" AS index`. Раньше маркеры
  оставались буквально (доходили до источника, напр. `%(index)s` → ошибка elastic). +4 офлайн-теста.
- **Elastic 200 с телом-ошибкой**: `data_taxi_requests`/`data_taxi_aggs_requests` теперь ловят
  `{"error":..., "status": 4xx/5xx}` при HTTP 200 (429/5xx → повтор, прочее → падение с текстом
  причины), а не «проглатывают» ошибку.

## 0.20. Полноценный AI-пайплайн (Фаза 4) в `v0.12.0`

Доведён агент NL→DSL до полноценного пайплайна (см. `app/llm.py`, `app/ai_pipeline.py`, `draw_ai`):
- **Активный финал**: из ответа извлекается последний блок ```harvester``` → кнопки «Открыть в Harvester»
  / «Выполнить» / «Сохранить как script-объект» (хуки `ui_show_panel`/`ui_harvester_load`/`ui_harvester_run`).
- **Агент пишет объекты** — действие `save_object` (только `script`, роли ≤ ролей пользователя) через
  обязательный **диалог подтверждения**; сохранение параметризовано (`create_new_object`/`_version`).
- **Сессионные лимиты + токены**: `agent_session_max_actions` (40) и `agent_session_token_budget`
  (200000) в Settings→AI; живой счётчик «действия/токены», жёсткая остановка цикла по лимиту;
  `llm_chat` возвращает usage.
- **UX**: кнопка **Стоп** (флаг между итерациями), **история чата** переживает reload
  (`app.storage.user["ai_conversation"]`), кнопка «Очистить»; **стриминг** ответа
  (`llm_chat_stream`, ollama NDJSON / openai SSE) с прогрессивным показом и фолбэком на нестрим.
- **Знания агента (промпт)**: модель данных (list of dict), корректный синтаксис APPLY (обяз. `[]`),
  порядок работы (list_objects → get_source_functions → run; проверять функции до вызова), правила
  in-memory SQL — кавычки для колонок с точками (`"custom_host.ip"`) и **экранирование `\"` внутри
  JSON-массива `queries`**.

Сопутствующие фиксы движка/коннекторов:
- **duckdb**: устойчивость к старому duckdb без `duckdb.typing` (UDF пропускаются с WARNING, обычный SQL
  работает); прицельная диагностика ошибок (тип + кадр traceback + сам SQL); защита пустого `queries`.
- **engine**: битый JSON-параметр (`list`/`dict`) больше не превращается молча в пустой `[]`/`{}` —
  возвращается реальная причина с подсказкой про экранирование кавычек.
- Офлайн-тесты: ~154 (парсер, cron, APPLY, DEF-инъекция, TheHive/CMDB, SQL-зависимости, AI-пайплайн).

## 1. Текущее состояние (актуально на `v0.12.0`)

### Работает (проверено на стенде)
- Запуск; **ленивая загрузка** зависимостей; секреты/конфиги — из окружения/аргументов/`pwinput` (не в коде).
- **Единая сигнатура коннекторов** `(parameters, source_object, data_map, current_state)`.
- **DSL-петля целиком**: `DEF` (с инъекцией `%(name)X`), `CALC`, `GET`/`GET script`/`GET APPLY`, `PRINT`,
  `SHOW(table|matplotlib)` (2D/3D, слои, стили, экспорт графа), `SAVE` (файл и `storage`), `LOAD`/`GET LOAD`
  (persistent-кэш с TTL), `NOTIFY`.
- **Параллельный исполнитель**: пул потоков по стадиям + параллельный `APPLY`, по-источниковые лимиты (`max_threads`).
- **История запусков** (`executions`, вкладка History) и **раздел «Хранилище»** (кэш).
- **Планировщик** (cron) — запуск сохранённых скриптов по расписанию, ALERT с результатом.
- **HTTP API** (`POST /api/script`, ключи) и **MCP-сервер**; **анализатор** выполнения (Mermaid + экспорт).
- **Settings** (тема/шрифты, пользователи/роли, AI, сети, API-ключи), **i18n** (RU/EN).
- **Развёртывание**: Docker (образ/compose), опциональный TLS, авто-версия в логах.
- **AI-режим (Фаза 4)**: полноценный пайплайн NL→DSL — агент исследует источники, пишет/проверяет
  скрипт через `run`, отдаёт финал в ```harvester``` (Открыть/Выполнить/Сохранить), пишет объекты с
  подтверждением, сессионные лимиты/токены, Стоп/история/стриминг.
- **Память агента (База знаний)**: общая таблица `knowledge`, раздел UI «База знаний», действия
  `memory_*` (авто-запись), авто-инъекция релевантных заметок в промпт.
- Тесты `tests/` (~168, офлайн): парсер, cron, APPLY-порядок, DEF-инъекция, TheHive/CMDB-хелперы,
  AI-пайплайн (вкл. память/ранжирование заметок).

### Осознанно принятые решения
- **SQL-инъекции в SQL-источниках — принятый риск** (доверенный круг, роли, сетевой whitelist).
  См. `ARCHITECTURE.md §10`. Управляющие INSERT объектов параметризованы.

### Не реализовано / техдолг
- **`universal_harvester:local_scenario`** ссылается на отсутствующие модули; **`grafana`/`teleport`** отключены.
- Нет **CI**, `pyproject.toml`/lock, Docker HEALTHCHECK.
- Движок/коннекторы/UI покрыты тестами частично (нужен вынос чистых функций из `interface.py`).
- TheHive — авто-пагинация и сущности cases/observables (бэклог).
- Опечатки/мелочи: `crptgrphy.py`, `nane`, `dynamica_agg_dict`.

## 2. Принципы развития
- **Сначала надёжность ядра, потом расширение.** Не строить AI/MCP поверх неустойчивого исполнения.
- **Обратная совместимость DSL** — скрипты пользователей не должны ломаться; новые возможности аддитивны.
- **Каждая новая возможность — с тестом** (хотя бы парсер/движок офлайн без БД).
- **MCP/агент — стратегическая цель**: source как инструмент, который вызывает и человек, и агент.

## 3. Фазы развития (статус)

> Фазы 1–3 и деплой из Фазы 5 — **выполнены** (см. разделы 0.9–0.19 выше и историю версий).
> Осталось: остаток **Фазы 5** (CI/качество). Ниже — исходные формулировки с отметками.

### Фаза 1. Завершение DSL-петли и устойчивость ядра  ✅ выполнено
Цель: скрипт пишется и полностью отрабатывает предсказуемо, ошибки видны.
- `SAVE` — сохранение результатов в таблицу `storage` (owner/execution/json) + чтение.
- `CALC` — базовые операции над переменными (числа/строки/даты), с тестами парсера и исполнения.
- Тесты движка `commands_executor` офлайн: граф зависимостей, `APPLY`, прерывание на ошибке, статусы.
- Мелкий техдолг: исправить опечатки имён (с осторожностью к обратной совместимости источников).
- **Критерий готовности**: типовой сценарий (несколько `GET` → SQL/pandas → `PRINT/SHOW/SAVE`) проходит, повторно открывается из `storage`, все ветки ошибок отражаются в панели шагов.

### Фаза 2. Выполнение и история  ✅ выполнено
Цель: масштабируемое и наблюдаемое выполнение.
- **История запусков** (`executions`): запись id/owner/timestamp/status/json; вкладка просмотра прошлых прогонов и их результатов.
- **Параллелизм по стадиям**: включить пул (нитями, не процессами — из-за UI-refs в `current_state`), задействовать `threads_limit`; унифицировать `max_threads`/`threads_limit`.
- Под-прогресс длинных коннекторов (например, постраничная выгрузка TheHive) → в панель шагов.
- **Критерий готовности**: сценарий с независимыми `GET` выполняется параллельно; прошлый прогон открывается из истории.

### Фаза 3. API / MCP-слой  ✅ выполнено (ядро)
Цель: source/script вызываются программно — это и есть «конструктор MCP».
- Оживить REST-эндпоинт: запуск сохранённого `script` по имени с параметрами, выдача результата (JSON/файл).
- Аутентификация для API (токен/ключ), авторизация по ролям, аудит вызовов.
- **MCP-сервер**: экспонировать каждый зарегистрированный source-`function` как MCP-tool со схемой параметров (из `required`), чтобы внешний агент мог их вызывать.
- **Критерий готовности**: внешний клиент/агент вызывает `script` и конкретный source через API/MCP с проверкой прав.

### Фаза 4. AI-режим (агент-сборщик сценариев)  ✅ выполнено (`v0.12.0`)
Цель: из запроса на естественном языке — готовый DSL-скрипт.
- ✅ Пайплайн «запрос → выбор источников/инструментов → сборка DSL → выполнение → ответ» (ReAct-агент,
  действия list_objects/get_source_functions/get_object/search_objects/run/save_object).
- ✅ Реестр источников как инструменты (по запросу, чтобы не раздувать контекст); активный финал
  (```harvester``` → Открыть/Выполнить/Сохранить), запись объектов с подтверждением, сессионные
  лимиты/токены, Стоп/история/стриминг.
- ✅ Долговременная память/контекст («Knowledge base»): общая (командная) база знаний в БД (таблица
  `knowledge`), раздел UI «База знаний», действия агента `memory_save/search/list/get/delete` (запись
  автоматическая), авто-инъекция релевантных заметок в системный промпт по теме запроса.
- **Критерий готовности**: по NL-запросу агент собирает корректный скрипт, исполняет его движком и
  возвращает результат с источниками — **подтверждено на стенде**.

### Фаза 5. Эксплуатация и качество  🔶 частично
- ✅ **Конфигурация без хардкода**: master key/конфиги — через env/аргументы/`pwinput`.
- ✅ `Dockerfile`/compose; ⬜ `pyproject.toml`/lock, healthcheck, CI (линт + `python -m unittest`).
- ✅ Наблюдаемость: структурные JSON-логи, тайминги шагов, авто-версия.
- 🔶 Расширение тестов: парсер/cron/APPLY/DEF покрыты; движок/коннекторы/UI — частично (вынести чистые
  функции из `interface.py` в модуль без `nicegui` для тестируемости).

## 4. Бэклог коннекторов (по мере потребности)
- Починить/перенести подсистему **сценариев** (`universal_harvester`).
- Вернуть **grafana**/**teleport** под актуальную сигнатуру и реестр.
- Новые источники по запросу (паттерн — `ADDING_SOURCES.md`); для TheHive — авто-пагинация и доп. сущности (cases, observables).

## 5. Сквозные направления
- **Безопасность**: в рамках принятой модели угроз; при расширении круга пользователей вернуться к параметризации `app/db.py` и валидации токенов (чеклист в `ARCHITECTURE.md`).
- **Документация**: поддерживать `ARCHITECTURE.md` актуальным; руководство пользователя по DSL (DEF/GET/APPLY/CALC/SAVE/PRINT/SHOW/NOTIFY) с примерами.

## 6. Открытые вопросы для приоритизации
Фазы 1–3 и деплой закрыты. Осталось согласовать:
1. **AI-режим (Фаза 4)**: локальные модели (ollama/llama) или внешний провайдер как основной? Глубина
   автосборки DSL (полный NL→DSL пайплайн vs текущий ReAct-агент).
2. **Качество**: приоритет CI/`pyproject`/HEALTHCHECK и декомпозиции `interface.py` под тесты.
3. **Бэклог коннекторов**: чинить ли `universal_harvester`/`grafana`/`teleport` и в каком порядке.

---
*Поддерживается по мере развития проекта.*
